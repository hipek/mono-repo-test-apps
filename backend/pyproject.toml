[project]
name = "backend"
version = "0.1.0"
description = "FastAPI HTTP API — login endpoint"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.136.3",
    "httpx2>=2.4.0",
    "pydantic>=2.13.4",
    "python-multipart>=0.0.32",
    "uvicorn>=0.49.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
