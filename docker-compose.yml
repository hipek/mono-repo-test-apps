services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - mono_repo_data:/data
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/code
    depends_on:
      - backend
    environment:
      - API_HOST=http://backend:8000
    restart: unless-stopped

volumes:
  mono_repo_data:
