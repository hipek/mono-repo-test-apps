services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - mono_repo_data:/app/mono_repo.db
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    environment:
      - API_HOST=http://backend:8000
    restart: unless-stopped

volumes:
  mono_repo_data:
